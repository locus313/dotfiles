{{- if eq .chezmoi.os "linux" }}
# Git Shortcuts
alias gs='git status'
alias ga='git add .'
alias gc='git commit'
alias gp='git push'

# network utilities
function pubip {
    curl -s http://ifconfig.me/ip
}

{{- end }}