[user]
	email = {{ .email }}
	name = {{ .name }}
{{- if eq .chezmoi.os "windows" }}
{{- if eq .chezmoi.data.oagmode "true" }}
[http]
    sslcainfo = ~/AppData/Roaming/custom-ca-bundle.pem
{{- end }}
{{- end }}
